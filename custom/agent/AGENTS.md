# 全局设定

## 网络与代理

- 当网络受限（请求超时、连接失败、DNS 解析失败等）时，先尝试通过代理 `127.0.0.1:10809` 访问网络（例如设置 `HTTPS_PROXY` / `HTTP_PROXY` 环境变量，或给 curl 等命令加 `-x http://127.0.0.1:10809`）。
- 若使用该代理仍然失败，停止反复重试，向用户说明失败原因，并请示用户切换代理路线（更换代理地址或端口）。
